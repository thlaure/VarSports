<div class="card mb-5 overflow-hidden border-0 rounded-0 article-spotlight">
    <div class="row g-0">
        <div class="col-md-4">
            <picture>
                <source srcset="{{ (asset('images/uploads/article/') ~ (article.image ? article.id ~ '/' ~ article.image : 'default_cover.webp')) | imagine_filter('cover_article_event_list_webp') }}" type="image/webp">
                <source srcset="{{ (asset('images/uploads/article/') ~ (article.image ? article.id ~ '/' ~ article.image : 'default_cover.webp')) | imagine_filter('cover_article_event_list_jpg') }}" type="image/jpeg">
                <img src="{{ asset('images/uploads/article/') ~ (article.image ? article.id ~ '/' ~ article.image : 'default_cover.webp') }}" alt="Couverture de l'article {{ article.title }}" class="img-fluid object-fit-cover w-100 article-cover-list" loading="lazy" width="390" height="140">
            </picture>
        </div>
        <div class="col-md-8">
            <div class="card-body px-0 pt-md-0 px-md-3">
                <h5 class="card-title text-varsports">{{ article.title }}</h5>
                <p class="card-text">{{ article.text|striptags|slice(0, 300)|raw|nl2br }}...</p>
                {% if article.slug %}
                    <a href="{{ path('app_article_show', { slug: article.slug }) }}" class="btn btn-sm btn-varsports rounded-0 stretched-link" aria-label="Lire l'article '{{ article.title }}'">Lire l'article</a>
                {% elseif article.externalLink %}
                    <a href="{{ article.externalLink }}" class="btn btn-sm btn-varsports rounded-0 stretched-link" aria-label="Lire l'article '{{ article.title }}'">Lire l'article</a>
                {% endif %}
            </div>
        </div>
    </div>
</div>
